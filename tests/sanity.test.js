import { describe, it, expect } from "vitest";

// Placeholder test to confirm the test runner and workflow are wired up
// correctly. Replace/remove once real tests exist for www/js/data.js
// (see GitHub issue #11 — the data layer this will actually test).
describe("test harness", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
